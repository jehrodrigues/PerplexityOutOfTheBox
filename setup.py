#!/usr/bin/env python3

from setuptools import find_packages, setup

setup(
    name='src',
    version='0.1',
    description="Bias detection with HolisticBias.",
    author='Jessica Silva',
    packages=find_packages(),
)